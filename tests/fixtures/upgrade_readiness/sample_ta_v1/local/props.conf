# Org customisation: additional field alias for src_ip
[source::syslog]
FIELDALIAS-src=src_host AS src_ip

# Custom sourcetype extraction
[source::custom_app]
TRANSFORMS-custom=custom_extract
EXTRACT-env=env=(?<environment>\w+)
